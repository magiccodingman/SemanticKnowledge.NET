# SemanticKnowledge.Quickstart

A minimal SQLite + local ONNX example. The first run may download the default embedding model through `OnnxTextEmbeddings.NET`.

```bash
dotnet run --project samples/SemanticKnowledge.Quickstart
```

Pass a database path as the first argument to use a different SQLite file.
